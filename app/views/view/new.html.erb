<%= form_for @todo, url: view_path, method: :create do |f| %>
	<%= f.label :tasks %>
	<%= f.text_field :tasks %><br/>

	<%= f.label :finished %>
	<%= f.text_field :finished %><br/>

	<%= f.submit %>
<% end %>
